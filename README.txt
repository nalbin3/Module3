This project demonstrates three basic cryptography concepts using simple Python scripts and OpenSSL:

1. **Hashing** (SHA-256 for strings and files)  
2. **Symmetric encryption** using a simple **Caesar substitution cipher**  
3. **Digital signatures** using RSA keys and OpenSSL (sign + verify)